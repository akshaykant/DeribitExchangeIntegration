package com.akshaykant.coppertest.repository;

import org.springframework.stereotype.Service;

@Service
public class UserBalanceRepositoryService {

    /*@Autowired
    private UserBalanceRepository userBalanceRepository;

    public List<UserBalance> listAllUser() {
        return userBalanceRepository.findAll();
    }

    public void saveUser(UserBalance userBalance) {
        userBalanceRepository.save(userBalance);
    }

    public UserBalance getUser(Integer id) {
        return userBalanceRepository.findById(id).get();
    }

    public void deleteUser(Integer id) {
        userBalanceRepository.deleteById(id);
    }*/
}
